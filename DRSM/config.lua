-- DRSM Telemetry Configuration
-- Modify these settings to match your setup.

return {
    ip       = "127.0.0.1",
    port     = 4135,
    version  = 2,
    log      = false,   -- set true for per-frame log output (noisy)
}
